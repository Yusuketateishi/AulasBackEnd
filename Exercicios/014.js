const AreaTriangulo = (altura, base) => {
    let AreaCalculo = (altura * base) / 2;
    return AreaCalculo
}

let resultado;;
resultado = AreaTriangulo(100,10);
console.log(`A área do triangûlo é ${resultado}`);
