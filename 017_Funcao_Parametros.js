const prompt = require('prompt-sync')();
let client = 'Roberto';
console.log(`Bem vindo a biblioteca ${client}`);
client = 'Patricia';
console.log(`Bem vindo a biblioteca ${client}`);
client = 'Jonana';
console.log(`Bem vindo a biblioteca ${client}`);

//Função solucionando o problema acima
function Biblioteca(convidado) {
    console.log(`Bem vindo a biblioteca ${cliente}`);
    return cliente
}

let cliente = prompt("Qual o nome do convidado?");
Biblioteca(cliente);